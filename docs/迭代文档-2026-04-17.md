# 迭代文档 2026-04-17

## 本轮目标

- 补齐搜索筛选与排序闭环。
- 补齐注销接口与前端登录态联动。
- 补齐数据集样本与版本相关接口。
- 补齐讨论评论回复链路。
- 补齐门户运营后台：推荐位与首页模块开关。
- 补齐内容运营后台：模板、案例、文档分类、文档、FAQ、视频教程管理。
- 修复现有 Wiki 测试失败，确保后端测试恢复通过。

## 已完成实现

### 1. 搜索能力增强

- 前端搜索页新增：
  - 资源类型筛选
  - 标签筛选
  - 机器人类型筛选
  - 排序切换
  - 最近时间筛选
- 后端搜索服务新增：
  - `updated_within` 时间范围过滤
  - 跨资源聚合后的统一排序
  - 全类型检索下的统一分页切片

### 2. 登录注销闭环

- 新增后端接口：
  - `POST /api/v1/auth/logout`
- 前端 `auth` store 改为优先调用注销接口，再清理本地令牌和用户信息。

### 3. 模型与讨论页增强

- 模型列表页新增：
  - 卡片/列表视图切换
  - 标签筛选
  - 排序
  - 分页
  - loading / error / empty 状态
- 讨论详情页新增：
  - 评论回复
  - 楼层归并显示
  - 回复目标提示与取消回复

### 4. 数据集链路补全

- 新增后端接口：
  - `GET /api/v1/datasets/{id}/samples`
  - `POST /api/v1/datasets/{id}/versions`
- 数据集详情页新增版本列表。
- 数据集上传页补充版本说明字段。

### 5. 文档中心增强

- 文档中心新增：
  - 目录搜索
  - 面包屑
  - 视频教程目录展示
- 新增公共接口：
  - `GET /api/v1/videos`

### 6. 门户运营后台

- 新增数据模型：
  - `home_module_settings`
  - `featured_resources`
- 新增后台接口：
  - `GET /api/v1/admin/portal/modules`
  - `PUT /api/v1/admin/portal/modules/{key}`
  - `GET /api/v1/admin/portal/featured-resources`
  - `POST /api/v1/admin/portal/featured-resources`
  - `PUT /api/v1/admin/portal/featured-resources/{id}`
  - `DELETE /api/v1/admin/portal/featured-resources/{id}`
- 新增后台页面：
  - `web/src/views/admin/AdminPortalView.vue`
- 首页已接入模块开关与推荐位数据。

### 7. 内容运营后台

- 新增数据模型：
  - `video_tutorials`
- 新增后台接口：
  - `GET /api/v1/admin/content/templates`
  - `POST /api/v1/admin/content/templates`
  - `PUT /api/v1/admin/content/templates/{id}`
  - `GET /api/v1/admin/content/application-cases`
  - `POST /api/v1/admin/content/application-cases`
  - `PUT /api/v1/admin/content/application-cases/{id}`
  - `GET /api/v1/admin/content/doc-categories`
  - `POST /api/v1/admin/content/doc-categories`
  - `PUT /api/v1/admin/content/doc-categories/{id}`
  - `GET /api/v1/admin/content/docs`
  - `POST /api/v1/admin/content/docs`
  - `PUT /api/v1/admin/content/docs/{id}`
  - `GET /api/v1/admin/content/faqs`
  - `POST /api/v1/admin/content/faqs`
  - `PUT /api/v1/admin/content/faqs/{id}`
  - `GET /api/v1/admin/content/videos`
  - `POST /api/v1/admin/content/videos`
  - `PUT /api/v1/admin/content/videos/{id}`
- 新增后台页面：
  - `web/src/views/admin/AdminContentView.vue`

### 8. 种子数据与兼容升级

- 自动迁移新增模型。
- 种子数据新增：
  - 首页模块默认配置
  - 推荐位默认数据
  - 视频教程默认数据
- `ensureV1SeedData` 支持旧库增量补种，不要求删库重建。

### 9. 测试修复

- 调整 Wiki 词条创建/更新 DTO 的 `content` 最小长度校验，修复既有用例 `TestV2WikiGovernanceFlow`。

### 10. 企业认证与内容治理增强

- 新增独立企业认证接口：
  - `POST /api/v1/enterprise-verifications`
- 个人中心在选择企业认证时，已改为走独立企业认证接口。
- 内容管理新增删除与批量状态操作：
  - 模板批量状态更新与删除
  - 案例批量状态更新与删除
  - 文档批量状态更新与删除
  - FAQ 删除
  - 视频教程批量启停与删除
- 新增后台运营操作日志：
  - `GET /api/v1/admin/operations`
- 内容管理页面新增：
  - 批量勾选
  - 批量状态更新
  - 删除操作
  - 操作日志页签

### 11. 回归测试补充

- 新增企业认证独立接口用例。
- 新增内容管理删除 / 批量状态 / 操作日志链路用例。

### 12. 数据集多媒体预览与分批下载任务

- 数据集样本模型扩展：
  - 支持样本文件路径
  - 支持样本文件名
- 数据集详情页新增：
  - 图片样本真预览
  - 视频样本占位预览与文件链接
  - 点云样本占位预览与文件链接
  - 文件样本链接
- 数据集上传页新增：
  - 多样本文件上传
  - 支持图片、视频、点云扩展名
- 新增分批下载任务模型：
  - `download_package_tasks`
- 新增接口：
  - `GET /api/v1/datasets/{id}/download-packages`
  - `POST /api/v1/datasets/{id}/download-packages`
- 分批下载任务会生成：
  - 总包说明文件
  - 多个分片下载链接
- 旧库增量补种：
  - 数据集多媒体样本
  - 图片 / 视频 / 点云占位样本文件

### 13. 数据集链路回归测试

- 新增多媒体样本返回用例。
- 新增分批下载任务创建与查询用例。

### 14. 真实文件切片与数据集配置模板化

- 分批下载任务从演示型分片升级为真实文件切片：
  - 使用真实数据集版本文件进行分片
  - 根据目标分片数按字节切片
  - 生成真实分片文件链接
  - 生成总包说明文件
- `LocalStorage` 新增文件切片能力，支持按相对路径切分已有文件。
- 新增公开数据集配置接口：
  - `GET /api/v1/datasets/options`
- 新增后台维护对象：
  - 协议模板 `agreement_templates`
  - 权限级别 `dataset_privacy_options`
- 新增后台接口：
  - `GET /api/v1/admin/content/agreement-templates`
  - `POST /api/v1/admin/content/agreement-templates`
  - `PUT /api/v1/admin/content/agreement-templates/{id}`
  - `DELETE /api/v1/admin/content/agreement-templates/{id}`
  - `GET /api/v1/admin/content/privacy-options`
  - `POST /api/v1/admin/content/privacy-options`
  - `PUT /api/v1/admin/content/privacy-options/{id}`
  - `DELETE /api/v1/admin/content/privacy-options/{id}`
- 数据集上传页已接入：
  - 权限级别下拉
  - 协议模板选择
  - 应用协议模板到协议文本

### 15. 数据集配置与切片下载回归

- `go test ./...` 已覆盖：
  - 多媒体样本预览返回
  - 下载分片任务生成
  - 企业认证独立接口
  - 内容管理删除 / 批量状态 / 操作日志

### 16. 内容治理批量删除与分类删除

- 内容管理新增批量删除能力：
  - 模板批量删除
  - 案例批量删除
  - 文档批量删除
  - 视频批量删除
- 文档分类新增删除接口：
  - `DELETE /api/v1/admin/content/doc-categories/{id}`
- 删除分类前会检查该分类下是否仍存在文档，避免脏数据。
- 前端内容管理页已补：
  - 批量删除按钮
  - 分类删除按钮
  - 删除失败时错误提示

### 17. 视频与点云更专业预览

- 视频样本从占位卡片升级为原生 `video` 播放器。
- 新增组件：
  - `web/src/components/PointCloudPreview.vue`
- 点云样本支持轻量预览：
  - 解析简单 ASCII `PLY / OBJ / PCD`
  - 投影到 canvas 进行 2D 点云预览
  - 保留原始文件下载链接

### 18. 本轮校验

- 前端：
  - `cd web && pnpm build`
  - 结果：通过
- 后端：
  - `cd server && go test ./...`
  - 结果：通过

### 19. 数据集下载审批闭环

- 新增数据集访问申请模型：
  - `dataset_access_requests`
- 新增用户侧接口：
  - `GET /api/v1/datasets/{id}/access-requests/me`
  - `POST /api/v1/datasets/{id}/access-requests`
- 新增后台接口：
  - `GET /api/v1/admin/datasets/access-requests`
  - `POST /api/v1/admin/datasets/access-requests/{id}/decision`
- 受限规则：
  - `public` 维持原有协议确认后下载
  - `internal / restricted` 需要审批通过后才能下载和生成分批下载任务
  - 资源所有者和管理员可直接访问
- 前端新增后台审批页：
  - `web/src/views/admin/AdminDatasetAccessView.vue`
- 数据集详情页新增：
  - 访问申请状态提示
  - 下载权限申请按钮
  - 审核结果描述

### 20. 内容治理继续增强

- 内容管理新增：
  - 模板批量删除
  - 案例批量删除
  - 文档批量删除
  - 视频批量删除
  - 文档分类删除
- 分类删除保护：
  - 若分类下仍有关联文档，则禁止删除并返回错误

### 21. 数据集预览增强

- 视频样本升级为原生播放器预览。
- 点云样本升级为轻量点云预览组件：
  - 解析 ASCII `PLY / OBJ / PCD`
  - 投影到 canvas 显示
- 仍保留原始文件下载链接，便于进一步专业工具查看。

### 22. 下载审批回归测试

- 新增受限数据集审批链路测试：
  - 未审批前下载拒绝
  - 提交申请
  - 后台审核
  - 审核通过后允许下载和生成分批任务

## 新增或变更的前端页面

- `web/src/views/home/SearchView.vue`
- `web/src/views/models/ModelListView.vue`
- `web/src/views/community/DiscussionDetailView.vue`
- `web/src/views/datasets/DatasetDetailView.vue`
- `web/src/views/datasets/DatasetUploadView.vue`
- `web/src/views/docs/DocsView.vue`
- `web/src/views/home/HomeView.vue`
- `web/src/views/admin/AdminPortalView.vue`
- `web/src/views/admin/AdminContentView.vue`
- `web/src/views/admin/AdminDashboardView.vue`
- `web/src/views/profile/ProfileView.vue`
- `web/src/views/admin/AdminContentView.vue`
- `web/src/views/datasets/DatasetDetailView.vue`
- `web/src/views/datasets/DatasetUploadView.vue`
- `web/src/components/PointCloudPreview.vue`
- `web/src/views/admin/AdminDatasetAccessView.vue`

## 新增或变更的后端能力

- 门户运营配置与推荐位管理
- 内容运营管理接口
- 数据集样本与版本接口
- 注销接口
- 视频教程公共接口
- 搜索过滤与排序增强
- 企业认证独立接口
- 内容治理删除、批量状态和后台操作日志
- 数据集多媒体样本预览与分批下载任务
- 数据集协议模板与权限级别配置
- 真实文件切片下载
- 内容治理批量删除与分类删除保护
- 视频与点云预览增强
- 数据集下载审批闭环

## 验证结果

- 前端构建：
  - `cd web && pnpm build`
  - 结果：通过
- 后端测试：
  - `cd server && go test ./...`
  - 结果：通过

## 当前仍未完全覆盖的需求

- 后台尚未补“模型推荐标签维护”等更细粒度运营配置。
- 内容管理尚未补更细角色权限约束和操作审批流。
- 点云预览当前是轻量 2D 投影，不是完整三维交互视图。
- 数据集审批目前是第一版流程，尚未扩展到更复杂的权限矩阵和申请历史管理。

## 建议下轮继续项

1. 为点云预览补完整三维交互视图。
2. 补模型推荐标签维护与更多后台运营配置。
3. 补内容管理更细角色权限约束和操作审批。
4. 扩展数据集访问审批为可追踪历史和更细权限矩阵。
